How to Run Data Collection Scripts
Prerequisites
Python 3.6 or higher
pymongo

You can install pymongo using pip:
pip install pymongo

Running the Data Collection Scripts
Navigate to the 4_data_collection directory.

Run the desired data collection script:
python collect_data.py

Script Structure
collect_data.py: Main script for collecting data from external sources and storing it in the database.
